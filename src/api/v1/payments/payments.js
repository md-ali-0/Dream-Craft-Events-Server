import OrderPayment from "../../../lib/payments/orderPayment.js";

const payments = (req, res) => {
    try {
        const result = OrderPayment

    } catch (error) {

    }
}

export default payments;